{
  "name": "nordrassil",
  "description": "Nordrassil, the Word Trie - a trie data structure implemented in vanilla JavaScript",
  "repository": {
    "type": "git",
    "url": "git@github.com:mgarbacz/nordrassil.git"
  },
  "scripts": {
    "dev": "./node_modules/.bin/vite app"
    "build": "./node_modules/.bin/vite build --outDir ./../dist app",
    "watch": "../node_modules/.bin/vite build --outDir ./../dist --watch app",
    "serve": "./node_modules/.bin/vite preview --outDir ./../dist app",
    "test": "./node_modules/.bin/istanbul cover --coverage ./node_modules/.bin/_mocha"
  },
  "devDependencies": {
    "chai": "1.8.x",
    "coveralls": "^2.11.2",
    "istanbul": "0.3.x",
    "mocha": "1.16.x",
    "mocha-lcov-reporter": "0.0.1",
    "vite": "^4.5.0"
  }
}
